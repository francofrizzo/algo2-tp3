// #include "red.h"
#include <iostream>
#include "aed2.h"
// #include "red.h"
#include "dicc_log.h"

using namespace aed2;
using namespace tp3;

int main() {
    // ab<int> izq = ab<int>(ab<int>(), 1, ab<int>());
    // ab<int> der = ab<int>(ab<int>(), 15, ab<int>());
    // ab<int> centro = ab<int>(izq, 5, der);
    // std::cout << centro.raiz() << std::endl;
    // std::cout << centro.izq().raiz() << std::endl;
    // // std::cout << centro.der().raiz() << std::endl;
    // std::cout << izq.raiz() << std::endl;
    // std::cout << der.raiz() << std::endl;
    return 0;
}